#include<stdio.h>
#include<conio.h>
void main()
{
	char a,e,i,o,u,ch,consonants;
	clrscr();
	printf("\n enter ch:");
	scanf("%c",&ch);

	if(a && e && i && o && u)
	{
		printf("\n %c is not consonants",ch);
	}
	else
	{
		printf("\n %c is consonants",ch);
	}
	getch();
}

