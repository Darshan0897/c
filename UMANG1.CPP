#include<stdio.h>
#include<conio.h>
void main()
{
	int units,paise;
	clrscr();
	printf("units = ");
	scanf("%d",&units);
	if(units<200)
	{
		paise=units*0.80;
		printf("total = %d",total);
	}
	else
	{
		if(units>200&&units<300)
		{
			paise=units*0.90;
			printf("total = %d",total);
		}
		else
		{
			if(units>300)
			{
				paise=
			}
		}
	}
	getch();
}
